# دليل إعداد موقع اتحاد قبائل العلاونة

## متطلبات النظام

- Python 3.8 أو أحدث
- pip (مدير حزم Python)

## خطوات الإعداد

### 1. إعداد البيئة الافتراضية (Virtual Environment)

```bash
# إنشاء بيئة افتراضية
python -m venv venv

# تفعيل البيئة الافتراضية
# على Windows:
venv\Scripts\activate

# على macOS/Linux:
source venv/bin/activate
```

### 2. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 3. إعداد قاعدة البيانات

```bash
# إنشاء ملفات الهجرة
python manage.py makemigrations

# تطبيق الهجرات
python manage.py migrate
```

### 4. إنشاء مستخدم إداري

```bash
python manage.py createsuperuser
```

### 5. إعداد البيانات الأولية

```bash
python manage.py setup_initial_data
```

### 6. تشغيل الخادم

```bash
python manage.py runserver
```

الموقع سيكون متاحاً على: http://127.0.0.1:8000

## الميزات المتاحة

### للمستخدمين العاديين:
- تصفح المقالات والأخبار
- البحث في المقالات
- تصفح المقالات حسب الفئة
- مشاهدة تفاصيل المقالات

### للمديرين:
- تسجيل الدخول/الخروج
- إدارة المقالات (إنشاء، تعديل، حذف)
- إدارة الفئات
- لوحة الإدارة Django

## الروابط المهمة

- الصفحة الرئيسية: http://127.0.0.1:8000/
- المقالات: http://127.0.0.1:8000/articles/
- تسجيل الدخول: http://127.0.0.1:8000/accounts/login/
- لوحة الإدارة: http://127.0.0.1:8000/admin/

## البيانات الافتراضية

بعد تشغيل `python manage.py setup_initial_data`، ستحصل على:

- مستخدم إداري: `admin` / `admin123`
- 5 فئات للمقالات
- 5 مقالات نموذجية

## إدارة المقالات

### إنشاء مقال جديد:
1. سجل دخولك كمدير
2. اذهب إلى "إضافة مقال" من القائمة العلوية
3. املأ البيانات المطلوبة
4. احفظ المقال

### تعديل مقال موجود:
1. اذهب إلى المقال
2. اضغط على "تعديل"
3. قم بالتعديلات المطلوبة
4. احفظ التغييرات

### حذف مقال:
1. اذهب إلى المقال
2. اضغط على "حذف"
3. أكد الحذف

## الفئات المتاحة

- أخبار القبيلة
- التراث والثقافة
- التعليم والتنمية
- التكنولوجيا والتحول الرقمي
- الفعاليات والأنشطة

## تخصيص الموقع

### إضافة فئة جديدة:
1. سجل دخولك في لوحة الإدارة Django
2. اذهب إلى "الفئات"
3. اضغط "إضافة فئة"
4. املأ البيانات المطلوبة

### تخصيص التصميم:
- ملفات CSS: `static/styles.css`
- ملفات JavaScript: `static/script.js`, `static/animations.js`
- القوالب: مجلد `templates/`

## استكشاف الأخطاء

### مشاكل شائعة:

1. **خطأ في الاستيراد**: تأكد من تفعيل البيئة الافتراضية
2. **خطأ في قاعدة البيانات**: قم بتشغيل `python manage.py migrate`
3. **مشاكل في الملفات الثابتة**: تأكد من وجود مجلد `static/`

### سجلات الأخطاء:
```bash
# تشغيل الخادم في وضع التطوير مع عرض الأخطاء
python manage.py runserver --settings=alawna_website.settings
```

## النسخ الاحتياطي

### نسخ احتياطي لقاعدة البيانات:
```bash
python manage.py dumpdata > backup.json
```

### استعادة النسخ الاحتياطي:
```bash
python manage.py loaddata backup.json
```

## النشر (Production)

### متطلبات النشر:
1. تعطيل وضع التطوير (`DEBUG = False`)
2. تغيير المفتاح السري
3. استخدام قاعدة بيانات إنتاج (PostgreSQL/MySQL)
4. إعداد خادم ويب (Nginx/Apache)
5. استخدام WSGI (Gunicorn/uWSGI)

### متغيرات البيئة للنشر:
```bash
export SECRET_KEY="your-production-secret-key"
export DEBUG=False
export DATABASE_URL="postgresql://user:pass@localhost/dbname"
```

## الدعم والمساعدة

للحصول على المساعدة أو الإبلاغ عن مشاكل:
- راجع وثائق Django الرسمية
- تحقق من سجلات الأخطاء في وحدة التحكم
- تأكد من تحديث المتطلبات

---

تم تطوير هذا الموقع لخدمة تراث قبيلة العلاونة العريق
